# Local development + CI tooling (testing the API and predictor).
-r requirements-api.txt
pytest>=8,<9
httpx>=0.27
# Market-price model training (train_market_model.py). On macOS also needs the
# OpenMP runtime: `brew install libomp`.
lightgbm>=4,<5
