# Production build (e.g. Vercel) talks to the deployed backend on Hugging Face.
# Local dev keeps using .env.development (http://127.0.0.1:8000).
VITE_API_BASE=https://hello-ryan-vancouver-property-value-api.hf.space
